#include<stdio.h>
int divisible(int n)
{
   
   if (n%3==0 && n%5==0)
   {
     printf("%d is divisible by both",n);   
   }
   else 
   {
       printf("%d is not divisible by both",n);      
   }
}
int main()
{
    
   divisible(45);

    
    
    return 0;
}
